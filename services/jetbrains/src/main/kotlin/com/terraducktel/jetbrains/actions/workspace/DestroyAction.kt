package com.terraducktel.jetbrains.actions.workspace

/** Triggers a `destroy` run on the selected workspace, after a type-the-name guard
 *  ([RunActions.trigger]). */
class DestroyAction : RunCommandActionBase("destroy")
