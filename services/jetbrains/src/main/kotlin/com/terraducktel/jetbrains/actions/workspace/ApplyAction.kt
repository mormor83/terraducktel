package com.terraducktel.jetbrains.actions.workspace

/** Triggers an `apply` run on the selected workspace, after a confirmation ([RunActions.trigger]). */
class ApplyAction : RunCommandActionBase("apply")
